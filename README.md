# Plexus
Plexus — a 3D codebase visualizer that maps your project's file structure and import relationships as a living, interactive node graph. Expand dependencies on click, trace blast radius, detect dead code and circular imports, all inside an immersive WebGL canvas.
